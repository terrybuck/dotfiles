Host sahand
	hostname sahand.cee.carleton.ca
	user matt
	ControlMaster auto
	ControlPath /home/matt/.ssh/tmp/%h_%p_%r
host gaea
	hostname 134.117.78.53
host kahar
	hostname 134.117.78.222
Host pontus
	hostname pontus.cee.carleton.ca
	#LocalForward 3129 pontus:3128
	ControlMaster auto
	ControlPath /home/matt/.ssh/tmp/%h_%p_%r
Host dena
	hostname dena.cee.carleton.ca
#	LocalForward 27000 sahand:27000
#	LocalForward 27001 sahand:27001
#	LocalForward 3389 kahar:3389
	LocalForward 5678 synology:5000
#	ControlMaster auto
#	ControlPath /home/matt/.ssh/tmp/%h_%p_%r

# gitolite user
host gitolite
	user git
	hostname sahand.cee.carleton.ca
	identityfile ~/.ssh/gitolite
host gm
	user gm1
	hostname dena.cee.carleton.ca
host armnssm
	user armnssm
	hostname dena.cee.carleton.ca
host newarmn
	user newarmn
	hostname dena.cee.carleton.ca
host ec
	user russellmat
	hostname remote.cmc.ec.gc.ca
	# The control master stuff allows the second connection
	# to bypass the first gateway
	ControlMaster auto
	ControlPath /home/matt/.ssh/tmp/%h_%p_%r
host git
	user git
	hostname localhost
host nci
	user ncirriga
	hostname ncirrigation.com
	port 18765
host dena1
	user matt
	ProxyCommand ssh -q dena -W %h:%p
	#ControlMaster auto
	#ControlPath /home/matt/.ssh/tmp/%h_%p_%r
host sabalan
	user matt
	ProxyCommand ssh -q pontus -W %h:%p
	#ControlMaster auto
	#ControlPath /home/matt/.ssh/tmp/%h_%p_%r
host tinder
	hostname localhost
	port 5555
	ControlMaster auto
	ControlPath /home/matt/.ssh/tmp/%h_%p_%r
host pof
	hostname localhost
	port 5556
	ControlMaster auto
	ControlPath /home/matt/.ssh/tmp/%h_%p_%r
host ntctrac
	hostname localhost
	port 7777
host player
	hostname jmdaly.ca
	ControlMaster auto
	ControlPath /home/matt/.ssh/tmp/%h_%p_%r
host eos
	hostname 192.168.2.33
	ControlMaster auto
	ControlPath /home/matt/.ssh/tmp/%h_%p_%r

